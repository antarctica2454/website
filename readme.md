# Antarctica 2454 bid website

Built from the `github-pages` branch; deployed via Github Pages to https://antarctica2454.org/

## Build process

This is (presently) a collection of files that get deployed directly to Github Pages without any intermediary build process.

## Workflow

Feature development:

1. branch off of `trunk`
2. open PR
3. get approval
4. merge to `trunk`

Deployments:

1. open PR merging `trunk` to `github-pages`
2. get approval
3. merge
4. wait for Github to do its thing
5. ???
6. announce the updates
